console.log('---------------');

const radius = 4;

let circumference = 2 * Math.PI * radius;
circumference = circumference.toFixed(2);
console.log(`Circumference of the circle = ${circumference}`);

let area_circle = Math.PI * (4 ** 2);
area_circle = area_circle.toFixed(2);
console.log(`Area of the circle = ${area_circle}`);